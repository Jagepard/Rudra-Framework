<?php
    use Rudra\Container\Facades\Rudra;
    $url = Rudra::config()->get('url');
?>
<main>
    <div class="container py-4">
        <!-- ─── HEADER ─────────────────────────────────────────── -->
        <header class="pb-3 mb-4 border-bottom">
            <a href="/" class="d-flex align-items-center text-dark text-decoration-none">
                <img src="<?= $url ?>/images/rudra.png" alt="Rudra Framework" width="5%" class="me-2">
                <span class="fs-4 fw-bold">Rudra Framework</span>
                <span class="badge bg-secondary ms-2">~15 MB</span>
                <span class="badge bg-info ms-1">PHP 8.3+</span>
            </a>
        </header>

        <!-- ─── HERO ───────────────────────────────────────────── -->
        <div class="p-5 mb-4 bg-dark text-white rounded-3">
            <div class="container-fluid py-4">
                <h1 class="display-4 fw-bold">Lightweight. Transparent. No magic.</h1>
                <p class="col-md-8 fs-5 mt-3">
                    A PHP framework built on the <strong>KISS principle</strong>.
                    Every tool is visible, every dependency is explicit.
                </p>
                <div class="mt-4 d-flex flex-wrap gap-2">
                    <span class="badge bg-light bg-opacity-10 text-white border border-light border-opacity-25 fs-6 px-3 py-2">📦 Porto Architecture</span>
                    <span class="badge bg-light bg-opacity-10 text-white border border-light border-opacity-25 fs-6 px-3 py-2">💉 Autowiring</span>
                    <span class="badge bg-light bg-opacity-10 text-white border border-light border-opacity-25 fs-6 px-3 py-2">🎯 PHP 8 Attributes</span>
                    <span class="badge bg-light bg-opacity-10 text-white border border-light border-opacity-25 fs-6 px-3 py-2">💾 Lightweight Query Builder</span>
                    <span class="badge bg-light bg-opacity-10 text-white border border-light border-opacity-25 fs-6 px-3 py-2">🚫 No hidden dependencies</span>
                </div>
            </div>
        </div>

        <!-- ─── GETTING STARTED ────────────────────────────────── -->
        <div class="p-4 mb-4 bg-light rounded-3">
            <h2 class="fw-bold">🚀 Getting started</h2>
            <pre class="bg-dark text-light p-3 rounded mt-3"><code>composer create-project --prefer-dist rudra/framework newapp
cd newapp
php rudra serve</code></pre>

                <code>git clone git@github.com:Jagepard/Rudra-Framework.git && cd Rudra-Framework && ddev start</code>
           
            <p class="text-muted small mt-2 mb-0">
                Database config: <code>config/setting.ddev.yml</code> (DDEV), <code>config/setting.local.yml</code> (local), or <code>config/setting.production.yml</code> (production)
            </p>
        </div>

        <!-- ─── CODE EXAMPLES ──────────────────────────────────── -->
        <h2 class="fw-bold mb-3">🎯 Declarative routing & autowiring</h2>
        <div class="row g-3 mb-4">
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-dark text-white"><small>Routing with PHP 8 Attributes</small></div>
                    <div class="card-body">
<pre class="m-0 small"><code class="language-php">#[Routing(url: 'user/:name')]
#[Middleware(name: AuthMiddleware::class)]
public function index(string $name = 'John'): void
{
    $this->info("Hello $name");
    render('layout', data());
}</code></pre>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-dark text-white"><small>Automatic Dependency Injection</small></div>
                    <div class="card-body">
<pre class="m-0 small"><code class="language-php">// Container resolves ALL dependencies automatically
public function show(
    UserRepositoryInterface $users,
    SmsSenderInterface $sms
): void {
    $user = $users->findById(1);
    $sms->send($user->phone, 'Hi!');
}</code></pre>
                    </div>
                </div>
            </div>
        </div>

        <!-- ─── DATA ACCESS LAYER ─────────────────────────────── -->
        <h2 class="fw-bold mb-3">💾 Lightweight Query Builder (not a heavy ORM)</h2>
        <div class="row g-3 mb-4">
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-dark text-white"><small>Entity — Zero Boilerplate</small></div>
                    <div class="card-body">
<pre class="m-0 small"><code class="language-php">class User extends Entity
{
    public static ?string $table = 'users';
}

// Works out of the box:
$users = User::getAll();
User::create(['name' => 'John']);</code></pre>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-dark text-white"><small>Fluent Query Builder</small></div>
                    <div class="card-body">
<pre class="m-0 small"><code class="language-php">$query = QB::select('id, name')
    ->from('users')
    ->where('status = :status')
    ->orderBy('created_at DESC')
    ->limit(10)
    ->get();

$results = User::qBuilder($query, ['status' => 'active']);</code></pre>
                    </div>
                </div>
            </div>
        </div>

        <!-- ─── ECOSYSTEM ─────────────────────────────────────── -->
        <h2 class="fw-bold mb-3">📦 Ecosystem Components</h2>
        <p class="fs-5">Modular architecture — use only what you need.</p>
        
        <div class="row g-3 mb-4">
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-primary text-white">🔐 Security</div>
                    <ul class="list-group list-group-flush small mb-0">
                        <li class="list-group-item"><strong>Auth</strong> — Authentication, RBAC, Remember Me</li>
                        <li class="list-group-item"><strong>OAuthClient</strong> — OAuth 2.0 (Yandex, VK, custom)</li>
                        <li class="list-group-item"><strong>Validation</strong> — Fluent validation, CSRF</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-success text-white">🎯 Core</div>
                    <ul class="list-group list-group-flush small mb-0">
                        <li class="list-group-item"><strong>Container</strong> — DI with 5 binding patterns</li>
                        <li class="list-group-item"><strong>Router</strong> — Attributes, middleware, REST</li>
                        <li class="list-group-item"><strong>Controller</strong> — Lifecycle hooks</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-info text-white">🛠️ Utils</div>
                    <ul class="list-group list-group-flush small mb-0">
                        <li class="list-group-item"><strong>EventDispatcher</strong> — Listeners + Observers</li>
                        <li class="list-group-item"><strong>View</strong> — PHP + Twig, file caching</li>
                        <li class="list-group-item"><strong>Pagination</strong> — SQL OFFSET/LIMIT</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- ─── CLI TOOLS ──────────────────────────────────────── -->
        <h2 class="fw-bold mb-3">🛠️ CLI Tools</h2>
        <p class="fs-5">Run <code>php rudra help</code> to see the full list.</p>

        <div class="row g-3 mb-4">
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-primary text-white">🚀 Launch & Help</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>php rudra serve</code> — Start dev server</li>
                        <li class="list-group-item"><code>php rudra help</code> — List all commands</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-success text-white">🛠️ Generators</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>make:container</code> 📦 · <code>make:controller</code> 🧭</li>
                        <li class="list-group-item"><code>make:model</code> 🧱 · <code>make:contract</code> 📜</li>
                        <li class="list-group-item"><code>make:factory</code> 🏭 · <code>make:middleware</code> 🛡️</li>
                        <li class="list-group-item"><code>make:listener</code> 👂 · <code>make:observer</code> 👁️</li>
                        <li class="list-group-item"><code>make:seed</code> 🌱 · <code>make:migration</code> 🗄️</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-warning text-dark">🗄️ Database</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>php rudra migrate</code> 🛤️ — Run pending migrations</li>
                        <li class="list-group-item"><code>php rudra seed</code> 🌾 — Execute seeders</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-info text-white">🔍 Debug</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>debug:router</code> 🗺️ — List all routes</li>
                        <li class="list-group-item"><code>debug:listeners</code> 🎧 · <code>debug:observers</code> 🔭</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-danger text-white">🔐 Security</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>php rudra bcrypt</code> 🔐 — Hash password</li>
                        <li class="list-group-item"><code>php rudra secret</code> 🔑 — Generate APP_KEY</li>
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-secondary text-dark">🧹 Maintenance</div>
                    <ul class="list-group list-group-flush small">
                        <li class="list-group-item"><code>cache:clear</code> 🧹 — Clear cache</li>
                        <li class="list-group-item"><code>convert:array-to-yml</code> 🔄 — PHP → YAML</li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- ─── FOOTER ─────────────────────────────────────────── -->
        <footer class="pt-3 mt-4 text-muted border-top text-center">
            <p class="mb-1">
                &copy; <a href="https://github.com/Jagepard" class="text-decoration-none">Jagepard</a> <?= date('Y') ?>
                · Licensed under <a href="https://mozilla.org/MPL/2.0/" class="text-decoration-none">MPL-2.0</a>
            </p>
        </footer>
    </div>
</main>
