paths:
    migrations: db/migrations
    seeds: db/seeds

environments:
    default_migration_table: phinxlog
    default_database: rudra
    rudra:
        adapter: mysql
        host: localhost
        name: rudra
        user: root
        pass: '123'
        port: 3306
        charset: utf8

version_order: creation
